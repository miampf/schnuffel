pub mod graph;
pub mod plugin;
pub mod types;

mod test {
    use crate::plugin::{NewState, Wrapper};

    #[test]
    fn test_plugin() {
        // TODO: once some plugin is written, write an e2e test as well
    }
}
